# 复盘四问

- 顺的地方：<what went smoothly>
- 卡的地方：<what got stuck>
- 返工点：<where rework happened>
- 下次项目内应改进之处：<next project-local improvement candidate>

适用说明：
- 若由 `退朝` 触发且本案使用过 `planning-with-files`，将本模板写入对应项目 worklog 的 `retrospective.md`
- 默认只记录项目内可复用经验，不直接升格为 canonical 或 agent 级规则
- 若有可复用经验，另写入 `learning-candidates.jsonl`；每行一个 JSON object，并显式写出 `promote_to`
